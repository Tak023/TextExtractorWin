<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TextExtractorWin.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextExtractorWin.Views">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="12" Padding="0,0,24,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Capture History" Style="{StaticResource TitleTextBlockStyle}" Grid.Column="0"/>
                <Button x:Name="ClearButton" Content="Clear History" Click="ClearButton_Click" Grid.Column="1"/>
            </Grid>

            <TextBlock x:Name="CountText" Text="0 items"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- History List -->
        <ListView x:Name="HistoryListView" Grid.Row="1"
                  SelectionMode="Single"
                  ItemClick="HistoryListView_ItemClick"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Text Preview -->
                        <TextBlock Text="{Binding TextPreview}"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="2"
                                   Grid.Column="0" Grid.Row="0"/>

                        <!-- Stats -->
                        <TextBlock Grid.Column="0" Grid.Row="1"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="12">
                            <Run Text="{Binding WordCount}"/>
                            <Run Text="words •"/>
                            <Run Text="{Binding CharacterCount}"/>
                            <Run Text="chars"/>
                        </TextBlock>

                        <!-- Timestamp -->
                        <TextBlock Text="{Binding TimeAgo}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="12"
                                   Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty State -->
        <StackPanel x:Name="EmptyState" Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="Collapsed">
            <FontIcon Glyph="&#xE81C;" FontSize="48"
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock Text="No capture history"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,12,0,0"/>
            <TextBlock Text="Press Ctrl+Shift+1 to capture text"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
